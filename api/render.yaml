services:
  - type: web
    name: dlens-spotlight
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
